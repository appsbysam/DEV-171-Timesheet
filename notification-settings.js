(function(){
 function build(){const actions=document.querySelector('.manager-menu-actions');if(!actions||document.getElementById('managerReminderSettingsBtn'))return;const b=document.createElement('button');b.id='managerReminderSettingsBtn';b.type='button';b.textContent='⏰ Automatic Reminders';actions.appendChild(b);const m=document.createElement('div');m.id='autoReminderModal';m.className='staff-modal no-print';m.hidden=true;m.innerHTML=`<div class="staff-modal-backdrop"></div><section class="staff-modal-dialog"><div class="staff-modal-header"><div><h2>Automatic Reminders</h2><p>Only staff missing today’s times will be reminded.</p></div><button id="autoReminderClose" type="button">×</button></div><div style="display:grid;gap:14px;padding:8px 0"><label><input id="autoReminderEnabled" type="checkbox"> Enable automatic reminders</label><label>Reminder time (Sydney time)<input id="autoReminderTime" type="time" value="19:00"></label><fieldset><legend>Reminder days</legend><div id="autoReminderDays" style="display:grid;grid-template-columns:repeat(2,1fr);gap:8px"></div></fieldset><label>Notification title<input id="autoReminderTitle" type="text" value="Staff Timesheet Reminder"></label><label>Message<textarea id="autoReminderMessage" rows="3">Reminder: please enter your times for today.</textarea></label><div id="autoReminderStatus"></div><button id="autoReminderSave" type="button" class="primary-button">Save Automatic Reminder Settings</button></div></section>`;document.body.appendChild(m);['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'].forEach(d=>document.getElementById('autoReminderDays').insertAdjacentHTML('beforeend',`<label><input type="checkbox" value="${d}" checked> ${d}</label>`));b.onclick=open;document.getElementById('autoReminderClose').onclick=close;m.querySelector('.staff-modal-backdrop').onclick=close;document.getElementById('autoReminderSave').onclick=save}
 async function open(){if(!(await requireManagerSession()))return;closeManagerMenu();document.getElementById('autoReminderModal').hidden=false;document.body.classList.add('staff-modal-open');const {data,error}=await db.rpc('manager_notification_settings',{p_token:managerSessionToken});if(error||!data?.success){status(data?.message||error?.message||'Unable to load settings.',true);return}document.getElementById('autoReminderEnabled').checked=!!data.enabled;document.getElementById('autoReminderTime').value=data.reminder_time||'19:00';document.getElementById('autoReminderTitle').value=data.title||'';document.getElementById('autoReminderMessage').value=data.message||'';document.querySelectorAll('#autoReminderDays input').forEach(x=>x.checked=(data.reminder_days||[]).includes(x.value));status(data.enabled?'Automatic reminders are ON.':'Automatic reminders are currently OFF.')}
 function close(){document.getElementById('autoReminderModal').hidden=true;document.body.classList.remove('staff-modal-open')}
 function status(t,e=false){const x=document.getElementById('autoReminderStatus');x.textContent=t;x.style.color=e?'#a11':'#17652a'}
 async function save(){const days=[...document.querySelectorAll('#autoReminderDays input:checked')].map(x=>x.value);if(!days.length){status('Select at least one reminder day.',true);return}status('Saving…');const {data,error}=await db.rpc('manager_update_notification_settings',{p_token:managerSessionToken,p_enabled:document.getElementById('autoReminderEnabled').checked,p_reminder_time:document.getElementById('autoReminderTime').value,p_reminder_days:days,p_title:document.getElementById('autoReminderTitle').value,p_message:document.getElementById('autoReminderMessage').value});if(error||!data?.success){status(data?.message||error?.message||'Unable to save.',true);return}status(data.message||'Saved.')}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',build);else build();
})();